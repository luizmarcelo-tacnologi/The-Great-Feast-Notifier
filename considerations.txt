PROBLEMS TO SOLVE
	aparently a valid API key isn't necessary to get the data i want, how, why, is it really that way?
	make a log cicling, to prevent the log from growing forever
PLANS FOR THE FUTURE
	maybe change the menu options' order
	config a proper installer for the program (with start menu folder, run on startup, and some other usefull features)
	change the AI banners and icon (Error.png;banner.png;hypixel.ico) to some proper thing, and maybe add some different banners too
	maybe try to make the wxPython menus better looking (main one, settings and logs)
	hunt for things to maybe make the program consume less RAM
	maybe, just maybe, migrate the GUI to C#, and even less prone to, move the entire thing to C#


