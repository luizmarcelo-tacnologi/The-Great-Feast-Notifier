PROBLEMS TO SOLVE
	windows security blocks the app and the installer saying it couldn't verify it's publisher
	do something to check if the user have microsoft's ARSO activated to delay the app start until log-in
	add a little delay to sending notifications after user log-in
PLANS FOR THE FUTURE
	make all the menus pretier
	change the AI banners and icon (Error.png;banner.png;hypixel.ico), and maybe add some different banners too
	hunt for things to maybe make the program consume less RAM
	maybe, just maybe, migrate the entire thing to C#
NOTES
	aparently a valid API key wasn't necessary to get the data i want. How, why, is it really that way? Am I that dumb to never have noticed that?