PROBLEMS TO SOLVE
	startup notification overlaping with error notification, the startup notification should only happen if no error is found
	check if the harvest feast calculations are actually working
PLANS FOR THE FUTURE
	config a proper installer for the program
	maybe, just maybe, migrate the GUI to C#, and even less prone to, move the entire thing to C#
	change the AI banners and icon (Error.png;banner.png;hypixel.ico) to some proper thing, and maybe add some different banners too
	crate some single instance protection
	make a somewhat decent logs and settings GUI